name: Trigger auto deployment for container-app-r3eznk7tdwczq

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '//**'
    - '.github/workflows/container-app-r3eznk7tdwczq-AutoDeployTrigger-149bfc07-c4a6-462e-b730-c61b2ce31952.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPR3EZNK7TDWCZQ_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPR3EZNK7TDWCZQ_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPR3EZNK7TDWCZQ_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPR3EZNK7TDWCZQ_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPR3EZNK7TDWCZQ_REGISTRY_PASSWORD }}
          containerAppName: container-app-r3eznk7tdwczq
          resourceGroup: appspacesfastapi8937b0f2
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


